# AutoWin Integration Context - État d'avancement

**Date**: 2025-10-07
**Tâche**: Intégration complète d'AutoWin dans l'indexer Ponder et le frontend

## ✅ DÉJÀ FAIT

### Frontend (COMPLET)
1. ✅ Ajout des adresses AutoWin dans `apps/web/src/config/contractsAddress.ts`
2. ✅ Création du hook `apps/web/src/hooks/vault/useAutoWinQuote.ts`
3. ✅ Modification de `apps/web/src/hooks/vault/useDoubleDeposit.ts` (multicall)
4. ✅ Modification de `apps/web/src/hooks/vault/useSingleDeposit.ts` (multicall)
5. ✅ Ajout du toggle AutoWin dans `apps/web/src/components/Vault/UserVaultDetail/depositForm.tsx`
6. ✅ Modification de `apps/web/src/components/Vault/UserVaultDetail/doubleSideForm.tsx`
7. ✅ Modification de `apps/web/src/components/Vault/UserVaultDetail/oneSideForm.tsx`
8. ✅ Ajout de `autoWinVault` prop dans `apps/web/src/pages/VaultDetailPage/page.tsx`

### Indexer (EN COURS)
1. ✅ Ajout du champ `autoWinVault` dans la table `stickyVault` (ligne 243)
2. 🔄 EN COURS: Ajout des 5 nouvelles tables AutoWin dans `ponder.schema.ts`

## 🔄 EN COURS D'APPLICATION

### Fichier: `apps/indexer/ponder.schema.ts`

**Modification 1 (✅ FAIT)**: Ligne 243
```typescript
autoWinVault: t.hex(), // Nullable reference to AutoWin vault
```

**Modification 2 (À FAIRE)**: Après ligne 244, ajouter ces 5 tables:

```typescript
// ============ AUTOWIN VAULTS ============

export const autoWinVault = onchainTable(
  "autoWinVault",
  (t) => ({
    id: t.hex().primaryKey(), // AutoWin vault address
    stakingToken: t.hex().notNull(), // StickyVault address
    implementation: t.hex().notNull(),
    createdAtTimestamp: t.bigint().notNull(),
    createdAtBlockNumber: t.bigint().notNull(),

    // Aggregated stats
    totalDeposits: t.numeric().notNull().default("0"),
    totalWithdrawals: t.numeric().notNull().default("0"),
    totalBgtClaimed: t.numeric().notNull().default("0"),
    totalCompoundFees: t.numeric().notNull().default("0"),
    totalBountyPaid: t.numeric().notNull().default("0"),
    claimCount: t.integer().notNull().default(0),
    depositCount: t.integer().notNull().default(0),
    withdrawCount: t.integer().notNull().default(0),

    // Performance metrics
    avgBgtPerClaim: t.numeric().notNull().default("0"),
    lastClaimTimestamp: t.bigint().notNull().default(0n),
    estimatedAPR: t.numeric().notNull().default("0"),
  }),
  (table) => ({
    stakingTokenIndex: index().on(table.stakingToken),
  }),
);

export const autoWinDeposit = onchainTable(
  "autoWinDeposit",
  (t) => ({
    id: t.text().primaryKey(), // tx hash + "#" + log index
    transaction: t.hex().notNull(),
    timestamp: t.bigint().notNull(),
    autoWinVault: t.hex().notNull(),
    user: t.hex().notNull(),
    assets: t.bigint().notNull(),
    shares: t.bigint().notNull(),
  }),
  (table) => ({
    vaultIndex: index().on(table.autoWinVault),
    userIndex: index().on(table.user),
    timestampIndex: index().on(table.timestamp),
  }),
);

export const autoWinWithdraw = onchainTable(
  "autoWinWithdraw",
  (t) => ({
    id: t.text().primaryKey(), // tx hash + "#" + log index
    transaction: t.hex().notNull(),
    timestamp: t.bigint().notNull(),
    autoWinVault: t.hex().notNull(),
    user: t.hex().notNull(),
    assets: t.bigint().notNull(),
    shares: t.bigint().notNull(),
  }),
  (table) => ({
    vaultIndex: index().on(table.autoWinVault),
    userIndex: index().on(table.user),
    timestampIndex: index().on(table.timestamp),
  }),
);

export const autoWinBgtClaim = onchainTable(
  "autoWinBgtClaim",
  (t) => ({
    id: t.text().primaryKey(), // tx hash + "#" + log index
    transaction: t.hex().notNull(),
    timestamp: t.bigint().notNull(),
    autoWinVault: t.hex().notNull(),
    bountySender: t.hex().notNull(),
    bgtRecipient: t.hex().notNull(),
    compoundAmount: t.bigint().notNull(),
    compoundFee: t.bigint().notNull(),
    tokenToRecipient: t.bigint().notNull(),
    tvlSnapshot: t.numeric().notNull().default("0"), // TVL at claim time
  }),
  (table) => ({
    vaultIndex: index().on(table.autoWinVault),
    timestampIndex: index().on(table.timestamp),
    senderIndex: index().on(table.bountySender),
  }),
);

export const autoWinDayData = onchainTable(
  "autoWinDayData",
  (t) => ({
    id: t.text().primaryKey(), // autoWinVault address + "-" + day id
    date: t.integer().notNull(),
    autoWinVault: t.hex().notNull(),

    totalDeposits: t.numeric().notNull().default("0"),
    totalWithdrawals: t.numeric().notNull().default("0"),
    totalBgtClaimed: t.numeric().notNull().default("0"),
    totalCompoundFees: t.numeric().notNull().default("0"),
    totalBountyPaid: t.numeric().notNull().default("0"),

    depositCount: t.integer().notNull().default(0),
    withdrawCount: t.integer().notNull().default(0),
    claimCount: t.integer().notNull().default(0),

    avgAPR: t.numeric().notNull().default("0"),
    avgTVL: t.numeric().notNull().default("0"),
  }),
  (table) => ({
    dateIndex: index().on(table.date),
    vaultIndex: index().on(table.autoWinVault),
    compoundIndex: index().on(table.autoWinVault, table.date),
  }),
);
```

**Modification 3 (À FAIRE)**: Dans `stickyVaultRelations` (vers ligne 948), ajouter:
```typescript
autoWinVaultRef: one(autoWinVault, {
  fields: [stickyVault.autoWinVault],
  references: [autoWinVault.id],
}),
```

**Modification 4 (À FAIRE)**: À la fin du fichier (après ligne 1032), ajouter:
```typescript
// AutoWin Relations
export const autoWinVaultRelations = relations(autoWinVault, ({ one, many }) => ({
  stakingTokenRef: one(stickyVault, {
    fields: [autoWinVault.stakingToken],
    references: [stickyVault.id],
  }),
  deposits: many(autoWinDeposit),
  withdraws: many(autoWinWithdraw),
  bgtClaims: many(autoWinBgtClaim),
  dayData: many(autoWinDayData),
}));

export const autoWinDepositRelations = relations(autoWinDeposit, ({ one }) => ({
  autoWinVault: one(autoWinVault, {
    fields: [autoWinDeposit.autoWinVault],
    references: [autoWinVault.id],
  }),
  transaction: one(transaction, {
    fields: [autoWinDeposit.transaction],
    references: [transaction.id],
  }),
}));

export const autoWinWithdrawRelations = relations(autoWinWithdraw, ({ one }) => ({
  autoWinVault: one(autoWinVault, {
    fields: [autoWinWithdraw.autoWinVault],
    references: [autoWinVault.id],
  }),
  transaction: one(transaction, {
    fields: [autoWinWithdraw.transaction],
    references: [transaction.id],
  }),
}));

export const autoWinBgtClaimRelations = relations(autoWinBgtClaim, ({ one }) => ({
  autoWinVault: one(autoWinVault, {
    fields: [autoWinBgtClaim.autoWinVault],
    references: [autoWinVault.id],
  }),
  transaction: one(transaction, {
    fields: [autoWinBgtClaim.transaction],
    references: [transaction.id],
  }),
}));

export const autoWinDayDataRelations = relations(autoWinDayData, ({ one }) => ({
  autoWinVault: one(autoWinVault, {
    fields: [autoWinDayData.autoWinVault],
    references: [autoWinVault.id],
  }),
}));
```

## 📋 TODO LIST RESTANTE

### Fichiers indexer à modifier:

1. ✅ `apps/indexer/ponder.schema.ts` (4 modifications - FAIT)
2. ✅ `apps/indexer/src/utils/abi.ts` - Ajouter exports AutoWin - FAIT
3. ✅ `apps/indexer/ponder.config.ts` - Ajouter 2 contrats - FAIT

### Fichiers à créer:

4. ✅ `apps/indexer/src/autowin/AutoWinDeployed.ts` - FAIT
5. ✅ `apps/indexer/src/autowin/Deposit.ts` - FAIT
6. ✅ `apps/indexer/src/autowin/Withdraw.ts` - FAIT
7. ✅ `apps/indexer/src/autowin/BgtClaimed.ts` - FAIT

### Frontend à faire:

8. ✅ GraphQL query updated in VaultDetailPage - FAIT
9. ✅ `apps/web/src/pages/VaultDetailPage/page.tsx` - Utiliser autoWinVault depuis GraphQL - FAIT

## ✅ INTÉGRATION COMPLÈTE

Tous les fichiers ont été créés et modifiés. L'intégration AutoWin est maintenant terminée:

**Indexer:**
- ✅ Schema avec 5 nouvelles tables AutoWin
- ✅ Table autoWinUserPosition pour tracker les positions utilisateur
- ✅ ABIs exportés dans utils/abi.ts
- ✅ Contrats configurés dans ponder.config.ts
- ✅ 4 event handlers créés (AutoWinDeployed, Deposit, Withdraw, BgtClaimed)
- ✅ Event handlers Deposit et Withdraw mettent à jour les positions utilisateur

**Frontend:**
- ✅ Toggle AutoWin dans les formulaires de dépôt
- ✅ Multicall pour déposer dans StickyVault + AutoWin en une transaction
- ✅ GraphQL query qui récupère l'adresse autoWinVault
- ✅ GraphQL query qui récupère les positions AutoWin de l'utilisateur
- ✅ Hook useAutoWinPosition pour fetch les données on-chain (balance, valeur USD)
- ✅ Component AutoWinPosition pour afficher la position AutoWin
- ✅ Affichage des deux positions (StickyVault + AutoWin) dans UserVaultDetail
- ✅ Transmission de autoWinVault aux formulaires de dépôt
- ✅ Refresh automatique après deposit/withdraw réussi via callback onSuccess

## 📦 CODE COMPLET POUR CHAQUE FICHIER

### `apps/indexer/src/utils/abi.ts`

**Ajouter après ligne 14:**
```typescript
export { autowinFactoryABI as AutoWinFactoryABI } from "./abis/AutowinFactory";
export { AutowinABI } from "../../../web/src/config/abis/Autowin";
export { autowinABI as AutoWinRouterABI } from "../../../web/src/config/abis/AutowinRouter";
```

**Ajouter dans CONTRACTS (après ligne 27):**
```typescript
AUTOWIN_FACTORY: "0x21b35a3dcF462540cb8EEA0e5d8594FF9e93C2e1" as const,
AUTOWIN_ROUTER: "0x674FC8955E28207E12156622DdeB875c7678A15c" as const,
```

### `apps/indexer/ponder.config.ts`

**Modifier imports (ligne 2):**
```typescript
import { CONTRACTS, FactoryABI, PoolABI, PositionManagerABI, StickyVaultFactoryABI, StickyVaultRouter, StickyVaultWithRouter, AutoWinFactoryABI, AutowinABI } from "./src/utils/abi";
```

**Ajouter dans contracts (après ligne 80):**
```typescript
autoWinFactory: {
  chain: "mainnet",
  abi: AutoWinFactoryABI,
  address: CONTRACTS.AUTOWIN_FACTORY,
  startBlock: 9742600
},
autoWinVaults: {
  chain: "mainnet",
  abi: AutowinABI,
  address: factory({
    address: CONTRACTS.AUTOWIN_FACTORY,
    event: {
      anonymous: false,
      inputs: [
        { indexed: true, name: "autoWin", type: "address" },
        { indexed: false, name: "implementation", type: "address" },
        { indexed: true, name: "stakingToken", type: "address" }
      ],
      name: "AutoWinDeployed",
      type: "event"
    },
    parameter: "autoWin"
  }),
  startBlock: 9742600
}
```

## 📁 FICHIERS COMPLETS À CRÉER

Voir les 4 fichiers event handlers dans le message de l'expert onchain-dex (id de message dans l'historique).

Les fichiers sont:
- AutoWinDeployed.ts
- Deposit.ts
- Withdraw.ts
- BgtClaimed.ts

## 🎯 STRATÉGIE DE REPRISE

1. Finir les 3 modifications restantes de `ponder.schema.ts`
2. Modifier `abi.ts` (2 ajouts)
3. Modifier `ponder.config.ts` (import + 2 contrats)
4. Créer le dossier `apps/indexer/src/autowin/`
5. Créer les 4 event handlers
6. Créer le hook frontend `useAutoWinVault.ts`
7. Modifier `VaultDetailPage/page.tsx`

## 📝 NOTES IMPORTANTES

- Les ABIs AutoWin sont déjà présents:
  - `apps/indexer/src/utils/abis/AutowinFactory.ts` ✅
  - `apps/web/src/config/abis/Autowin.ts` ✅
  - `apps/web/src/config/abis/AutowinRouter.ts` ✅

- Le frontend multicall est déjà implémenté et fonctionnel

- Adresses des contrats:
  - AUTOWIN_FACTORY: 0x21b35a3dcF462540cb8EEA0e5d8594FF9e93C2e1
  - AUTOWIN_ROUTER: 0x674FC8955E28207E12156622DdeB875c7678A15c
  - StartBlock: 9742600

- La stratégie est:
  - Indexer = historique seulement
  - Frontend = fetch on-chain pour données actuelles (TVL, balances, etc.)
